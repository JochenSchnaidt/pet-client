server:
  port: 8090

spring:
  application.name: pet-client
  profiles.active: default

#  cloud.config:
#    discovery:
#      enabled: false
#      service-id: configserver

    #uri: http://localhost:8090
#    label: development
#    username: root
#    password: s3cr3t
#    fail-fast: true

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}

logging:
  level:
    root: INFO
    org.springframework.web: INFO
    com.netflix.discovery: WARN
